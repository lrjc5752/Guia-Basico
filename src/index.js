import React from 'react';
import ReactDOM from 'react-dom'; // es la libreria que funciona en la Web, version de internet
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

